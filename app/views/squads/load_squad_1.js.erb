<% 
  @game = Game.new 
  ff = ''
  form_for(@game) do |f|
    ff = f
  end
%>
var squad_id = <%=@squad_id%>
$('#game_squad_id_1').val(squad_id)
$("#global-modal").modal("hide");
$('#team1-data').html("<%= escape_javascript(render partial: 'load_squad_1', locals: {f: ff, game: @game}) %>");