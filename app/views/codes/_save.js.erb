$("ul.errors").html("")
<% if @code.errors.any? %>
  <% @code.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>  
  $('#codes-table').DataTable().ajax.reload();
  $("#global-modal").modal("hide")
<% end %>